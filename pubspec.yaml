name: housing_complex_objects
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.6
  go_router: ^13.2.4
  dio: ^5.4.3+1
  freezed_annotation:
  json_annotation:
  flutter_bloc: ^8.1.5
  storage_utility: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed:
  build_runner:
  json_serializable:

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/icon.png
    - assets/mock_scheme.png
    - assets/status_complete.png
    - assets/status_incomplete.png